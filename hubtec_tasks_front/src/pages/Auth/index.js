import React, { Component } from 'react';
import Login from './Login';

export default class Auth extends Component{
    render() {
        return(
            <div className="Login">
                <Login/>
            </div>
        );
    }
}
